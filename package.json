{
	"name": "mcp",
	"version": "0.0.0",
	"private": true,
	"type": "module",
	"scripts": {
		"check": "npm test && npm run type-check && oxlint && npm run test:oauth",
		"test": "node --experimental-test-module-mocks --test tests/*.test.mjs",
		"test:oauth": "wrangler deploy --dry-run --outdir .wrangler/oauth-build && node tests/oauth.integration.mjs",
		"auth:rotate": "node scripts/set-auth-password.mjs",
		"deploy": "wrangler deploy",
		"dev": "wrangler dev",
		"lint:fix": "oxlint --fix",
		"cf-typegen": "wrangler types --env-file .env.example",
		"type-check": "tsc --noEmit"
	},
	"dependencies": {
		"@cloudflare/workers-oauth-provider": "^0.10.3",
		"@modelcontextprotocol/server": "2.0.0",
		"imapflow": "^2.0.0",
		"postal-mime": "^3.0.0",
		"uuid": "^14.0.2",
		"zod": "^4.4.3"
	},
	"devDependencies": {
		"@types/node": "^26.2.0",
		"miniflare": "^5.20260910.0-alpha",
		"oxlint": "^1.71.0",
		"typescript": "6.0.3",
		"wrangler": "^4.125.0"
	}
}
